meta {
  name: Get Budgets
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/accounting/budgets
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data: isArray
}
