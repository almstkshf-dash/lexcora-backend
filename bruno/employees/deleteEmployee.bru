meta {
  name: Delete Test Employee
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/employees/{{testEmployeeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
